# Import Razorpay configuration from separate properties file
spring.config.import=optional:classpath:razorpay.properties

spring.application.name=stud-erp
server.port=8787

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/college_erp
spring.datasource.username=root
spring.datasource.password=${DB_PASSWORD:Chinu@2005}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=true

# JSON Serialization
spring.jackson.serialization.fail-on-empty-beans=false

# SMTP Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:tridentacademyoftechnology05@gmail.com}
spring.mail.password=${MAIL_PASSWORD:ylxe aawi gfkw ayim}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.debug=true

# File Upload Configuration
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=50MB
spring.servlet.multipart.enabled=true

# Cloudinary Configuration
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:drixr9wtk}
cloudinary.api-key=${CLOUDINARY_API_KEY:138248573828358}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:QeexyoHmBsSRPCraUJbY1oSme8A}

# Note: Razorpay configuration is loaded from razorpay.properties
# Edit razorpay.properties to add your Razorpay API keys